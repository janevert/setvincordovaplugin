repositories {
    mavenCentral()
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    api(name: 'truckmessagesdk-debug', ext: 'aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}
